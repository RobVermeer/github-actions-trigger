name: Continues Integration

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types: [opened, ready_for_review, synchronize, labeled]

jobs:
  tests:
    name: Tests
    runs-on: ubuntu-20.04
    if: github.event.pull_request.draft != true || github.event.label.name == 'run'
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run tests
        run: echo "Hello, World"
